name: Snowflake CI/CD

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

env:
  SNOWFLAKE_ACCOUNT: ${{ vars.SNOWFLAKE_ACCOUNT }}
  SNOWFLAKE_USER: CICD_DEMO_GITHUB_USER

jobs:
  plan-on-pr:
    name: Plan (Preview Changes)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          use-oidc: true

      - name: Test connection
        run: snow connection test -x

      - name: Plan changes
        working-directory: dcm
        run: snow dcm plan --target DEMO -x

  deploy-on-merge:
    name: Deploy
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          use-oidc: true

      - name: Test connection
        run: snow connection test -x

      - name: Deploy
        working-directory: dcm
        run: snow dcm deploy --target DEMO -x
